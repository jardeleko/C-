#include <iostream>
#include <vector>
using namespace std;

int _xo = 0;
int size = _xo.size();

for (int i = size; i >= 0; i--){
	int tmp = n /_xo[i];
	co
}
int main(void){
	vector <int> coins;
	int n;
	n = 26;
	coins.push_back(20);
	coins.push_back(1);
	coins.push_back(2);
	coins.push_back(5);
	coins.push_back(10);

	vector<int> :: iterator it;


	for (it = coins.begin(); it != coins.end(); ++it){
		cout << *it << endl;
	}

	return 0;
}